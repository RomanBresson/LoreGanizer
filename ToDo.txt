Accessibility:
-Edit timeline style (dotted/dashed) for easier intelligibility/not relying on color only
-change font size

Misc:
-Add time scale at the bottom
-Add colors next to events to show quickly whose timelines they belong too
-Add local dilation for timeline with highly non-uniform event distributions
-Be able to select and shift (date/height) a set of events at the same time
-Zoom with mouse-wheel (one for date, one for height)
